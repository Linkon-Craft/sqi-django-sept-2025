from django.shortcuts import render, HttpResponse

# Create your views here.

def dashboard_view(request):
    return HttpResponse()

# Add base directly in settings
# create templates to store html 
# 

# add to installed apps in settings 